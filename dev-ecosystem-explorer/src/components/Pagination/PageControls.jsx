// PageControls is a placeholder — InfiniteScrollTrigger is the
// primary pagination strategy. This exists in the folder structure
// as specified but we delegate to InfiniteScrollTrigger.

export default function PageControls() {
  return null
}
